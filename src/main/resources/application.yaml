spring:
  application:
    name: filehost

server:
  error:
    whitelabel:
      enabled: false
sftp:
  port: 2222
  username: admin
  password: changeme
  password-auth-enabled: true
  authorized-keys-file: ./authorized_keys